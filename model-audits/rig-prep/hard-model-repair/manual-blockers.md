# Manual blockers

- `amazing-spider-man-2`: Browser loads the candidate structurally, but every camera/material/frustum/source-reexport/raw-normalized visual attempt renders only the stage floor. Source re-export still has texture pack warnings and did not produce visible mesh in browser. Next: Do not activate. Preserve as deferred source/material visibility blocker; next repair should inspect exported GLB materials/skin visibility in Blender and browser GLTF inspector before another conversion.
- `terminator-t-800`: Raw-normalized browser attempt shows at most a tiny speck. Diagnostics show extreme source bounds/scale and source re-export has missing texture warnings; current candidate is not visually usable. Next: Do not activate. Needs source-scale cleanup and texture/material repair before another VRM conversion.
- `spider-man-no-way-home`: Conversion and raw loader can complete, but camera/material/frustum/source-reexport/raw-normalized visual attempts still render only the stage floor. Next: Do not activate. Needs source orientation/mesh/material visibility diagnosis outside the browser smoke path.
- `jack-sparrow`: Duplicate copy can be hidden in the raw visual harness, but the visible copy does not deform when matched bones are posed. Source contains duplicated mesh/armature content that still needs proper cleanup or rebinding. Next: Preserve as reference evidence only. Do not activate until duplicate source cleanup and skin binding are repaired in Blender/export path.
- `elsa`: VRM loader rejects the candidate because required humanoid lower-body bones are missing; raw GLTF load succeeds but remains visually blank after normalization. Source audit shows only upper-body useful mapping plus duplicate/reflection source content. Next: Do not activate. Requires manual artist/source repair to add or recover lower-body bones before VRM humanoid conversion can be trusted.
- `buzz-lightyear`: No rig blocker after manual map, but source texture paths are missing so the VRM appears mostly white/material-degraded. Bone names are anonymous, so the mapping is position-derived and should be reviewed before public promotion. Next: Accept as active generated-avatar candidate with material and mapping caveats; do not public-promote without texture recovery and manual rig review.
- `teal-v2`: Minor texture pack warnings remain, but browser VRM load, visibility, and pose-suite evidence pass after manual bone-map recovery. Next: Safe to cherry-pick as a generated-avatar active candidate only; keep query-param-only and disabled in public UI until explicitly promoted.
